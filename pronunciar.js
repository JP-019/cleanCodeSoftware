function cal(p) {
    let t = p * 0.15;
    return p + t ;
}
console.log(cal(100));


function calcular(recibeNumero) {
    let tiempo = recibeNumero * 0.15;
    return recibeNumero + tiempo ;
}
console.log(calcular(100));
